class configuration:
    
    #read the config location table
    def initConfig(configFile):
        return 0
    
    #load config information from Metadata Table
    def loadConfig(configData):
        return 0
    
    def validateConections(configData):
        return True
        
        